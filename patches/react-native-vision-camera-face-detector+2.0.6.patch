diff --git a/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt b/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt
index 5d116a9..b9ff300 100644
--- a/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt
+++ b/node_modules/react-native-vision-camera-face-detector/android/src/main/java/com/margelo/nitro/camera/facedetector/HybridImageFaceDetector.kt
@@ -21,17 +21,23 @@ class HybridImageFaceDetector(
     options.toMLFaceDetectorOptions()
   )
 
+  // FIX (real-device finding, 2026-07-15): `image` here is always an
+  // `InputImage` -- a Nitro-generated Kotlin `sealed class` wrapping the
+  // TypeScript union `string | number | ImageUri` (see nitrogen/generated/
+  // .../InputImage.kt: `sealed class InputImage { data class First(String);
+  // data class Second(Double); data class Third(ImageUri) }`) -- never a
+  // raw Kotlin String or Map. The original `when (input) { is String ->
+  // ...; is Map<*,*> -> ...; else -> throw ... }` could therefore NEVER
+  // match either branch and always threw "Invalid image type", regardless
+  // of what the JS caller passed (confirmed on-device: every single
+  // detectFaces() call failed identically). Using InputImage's own
+  // `asFirstOrNull()`/`asThirdOrNull()` unwraps the sealed class correctly.
   private fun resolveInputImage(
-    input: Any?
+    input: InputImage
   ): String {
-    return when (input) {
-      is String -> input
-      is Map<*, *> -> {
-        val uri = input["uri"] as? String
-        uri ?: throw IllegalArgumentException("Invalid image object: missing 'uri'")
-      }
-      else -> throw IllegalArgumentException("Invalid image type. Expected string or { uri }")
-    }
+    input.asFirstOrNull()?.let { return it }
+    input.asThirdOrNull()?.let { return it.uri }
+    throw IllegalArgumentException("Invalid image type. Expected string or { uri }")
   }
 
   private fun createInputImage(uri: String): MLInputImage {
diff --git a/node_modules/react-native-vision-camera-face-detector/src/hooks/useFaceDetectorOutput.ts b/node_modules/react-native-vision-camera-face-detector/src/hooks/useFaceDetectorOutput.ts
index 3ae1a88..f7814c3 100644
--- a/node_modules/react-native-vision-camera-face-detector/src/hooks/useFaceDetectorOutput.ts
+++ b/node_modules/react-native-vision-camera-face-detector/src/hooks/useFaceDetectorOutput.ts
@@ -73,6 +73,22 @@ export function useFaceDetectorOutput( {
   const stableOnError = useRef( onError )
   stableOnError.current = onError
 
+  // DIAGNOSTIC/FIX (real-device finding, 2026-07-15): `options` is built via
+  // object-rest destructuring in this function's own parameter list, which
+  // creates a BRAND NEW object reference on every single call -- so
+  // `useMemo`'s `[options]` dependency never actually memoizes anything;
+  // this hook returns a new CameraOutput on every render of the calling
+  // component, for any reason. Consumed as `outputs={[faceDetectorOutput,
+  // ...]}` on a `<Camera>`, that constant identity churn makes
+  // useCameraController's outputs-array memoization (which the library's
+  // own docs say outputs "have to be explicitly memoized" for) see a
+  // "changed" outputs array on every render, triggering an expensive
+  // CameraX session reconfigure each time -- confirmed on-device to kill an
+  // in-progress video recording (CameraX can't carry a recording across a
+  // session rebind) within ~1s, regardless of what else was recording.
+  // Stringifying is the same idiom already used for this exact class of
+  // problem in react-native-vision-camera's own useCameraController.ts.
+  const optionsKey = JSON.stringify( options )
   return useMemo(
     () => createFaceDetectorOutput( {
       onFacesDetected( faces ) {
@@ -84,7 +100,8 @@ export function useFaceDetectorOutput( {
       outputResolution: outputResolution,
       ...options
     } ),
-    [ options ]
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+    [ optionsKey ]
   )
 }
 
