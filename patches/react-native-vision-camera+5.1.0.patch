diff --git a/node_modules/react-native-vision-camera/android/src/main/java/com/margelo/nitro/camera/hybrids/recording/HybridVideoRecorder.kt b/node_modules/react-native-vision-camera/android/src/main/java/com/margelo/nitro/camera/hybrids/recording/HybridVideoRecorder.kt
index 78cc48c..3969ccf 100644
--- a/node_modules/react-native-vision-camera/android/src/main/java/com/margelo/nitro/camera/hybrids/recording/HybridVideoRecorder.kt
+++ b/node_modules/react-native-vision-camera/android/src/main/java/com/margelo/nitro/camera/hybrids/recording/HybridVideoRecorder.kt
@@ -56,6 +56,7 @@ class HybridVideoRecorder(
         pendingRecording.start(executor) { event ->
           when (event) {
             is VideoRecordEvent.Start -> {
+              android.util.Log.e("VCDiag", "Start event fired")
               promise.resolve()
               didResolve = true
             }
@@ -66,6 +67,19 @@ class HybridVideoRecorder(
               // flips to false even when the finalize was auto-triggered (e.g. by
               // reaching `maxDuration` / `maxFileSize`) instead of via `stopRecording()`.
               this.recording = null
+              // DIAGNOSTIC (temporary, 2026-07-15): investigating a 0-byte
+              // output file on a real device despite a "successful" Finalize
+              // -- logging the raw event.error + recordingStats here to see
+              // whether didResolve (Start ever fired) and numBytesRecorded
+              // actually back up a real recording, or whether this is
+              // ERROR_SOURCE_INACTIVE firing near-instantly with zero bytes.
+              android.util.Log.e(
+                "VCDiag",
+                "Finalize: error=${event.error} didResolve=$didResolve " +
+                  "bytes=${event.recordingStats.numBytesRecorded} " +
+                  "durationNanos=${event.recordingStats.recordedDurationNanos} " +
+                  "cause=${event.cause}"
+              )
               if (isCancelled) {
                 // Recording was cancelled - delete the file and don't notify
                 isCancelled = false
